# WIP — 지적 로그 4건 공개 (2026-08-01 사용량 한도로 중단)

## 하려던 것

첫 무인 실전(05:02, PR #82)에서 나온 **지적 로그 4건을 GitHub에 올리기.**

## 중단 지점

민감값 검사까지 가지 못하고 사용량 한도(stop)에 걸렸다. **아무것도 커밋·푸시하지 않았다.**

## 먼저 판단해야 할 것 — 왜 그냥 못 올리나

리뷰는 **민감값 게이트보다 앞에서** 돈다(`review_post` → `write_post_file` → 검열). 그래서 지적의
인용 스팬은 **아직 검열을 안 거친 초안**에서 온 것이다. `sensitive_leak` 지적이라면 스팬 자체가
유출값일 수 있다 — 그래서 `log_critiques`가 "VM-private: spans never reach the repo"로 설계됐다.

오늘 4건은 `long_sentence`×2 · `vague_fact` · `other:긴문장`이라 안전코드가 없고 스팬도 이미 발행된
글에서 온 문장이라 **아마 안전하다.** 다만 확인 없이 올리지 말 것.

## 이어서 할 일

- [ ] 스팬·note에 `redaction_hits`를 돌려 민감값 0건 확인. 안전코드(`sensitive_leak`·
      `fabricated_texture`) 지적이 섞였으면 그 건은 스팬을 빼고 코드만 올린다.
      ```
      cd ~/projects/devbox && source vm/devlog-auto-retro.sh   # 소싱 가드 있음, main 안 돎
      # 스팬+note를 임시 파일로 뽑아 redaction_hits 실행
      ```
- [ ] devlog `docs/todo/2026-08-01_writing-feedback-live.md`에 **"첫 실전 관측" 절**로 4건을 적는다
      (로그 파일 자체를 커밋하지 말 것 — VM 비공개가 설계다. 관측 기록으로 옮겨 적는 형태).
- [ ] `/prmain`으로 반영.

## 같이 남길 관측

- `other:긴문장`이 나왔는데 어휘엔 이미 `long_sentence`가 있다. **없는 코드가 아니라 있는 코드를
  모델이 못 찾은 것** — 어제 채점의 "어휘 밖 19%"와는 성격이 다르다. 1회는 신호가 아니니
  며칠 더 반복되는지 보고, 반복되면 리뷰 프롬프트의 `long_sentence` 설명을 손본다.
- 승급 첫 기회는 **내일**이다(같은 코드가 2편에서 나와야 함, `long_sentence`가 유력).
  PR 본문에 `## 이번 회차 자가개선` 절이 처음 붙는지 본다.

## 오늘 확인된 것 (재확인 불필요)

- 05:02 자동 회고: `리뷰 완료 (지적 4건)` · `hugo 빌드 OK` · PR #82 자동 발행. 지적 로그 0→4건.
- 07:34 댓글봇: **백필 첫 발동** — `devbox/2026-07-29`(창 밖, 스레드 없음)를 건져 5편 29턴, PR #83 발행.
- 발행 사이트 `posts/devlog/2026-07-31/` HTTP 200.
