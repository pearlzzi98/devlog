# 2026-06-18 — 예약 게시 cron + 17일 글 정렬

17일 글이 18일 아침에 안 올라온 걸 사용자가 발견 → 원인 추적 후 구조 수정, 그리고 17일 글이 계약을 안 따른 것도 정렬.

## 버그수정

- [x] 17일 글 미공개 원인 규명·복구 — PR #2가 17일 23:45 KST 머지될 때 글 `date`(18일 07:00)가 미래라 Hugo가 제외(`buildFuture` 미설정), 이후 push가 없어 재빌드도 없었음. `workflow_dispatch` 수동 재빌드로 즉시 공개(200 확인).

## 개선

- [x] 예약 게시 실동작용 cron 추가 — `.github/workflows/pages.yml` `on.schedule: "5 22 * * *"`(07:05 KST). 미래 시각 글이 push 없이도 시각 경과 후 자동 공개됨. (PR #4)
- [x] 17일 글을 다이제스트 계약 구조로 정렬 — `content/posts/2026-06-17.md`. 임의 구조(`### 1.~4.` + `## 다음엔 이렇게`) → 표준(`### 한 일` / `### 막힌 것, 고친 것` / `### 돌아보며`) + `## devbox` 아래 `>` 한 줄 요약, 딱딱한 표현을 쉬운 일상어로. 사실 변경 없음. (PR #5)
- [x] CLAUDE.md·README.md에 일일 cron 재빌드(예약 게시 메커니즘) 반영.

## 메모

- 공개 사이트 PII/denylist 전수 점검(추적 파일 전체) — 0건. repo는 PUBLIC.
- 읽기 시간 표시(`N min`)는 PaperMod `ShowReadingTime` 기능, 자동 계산. 끄지 않고 유지하기로 함.
- cron 첫 발화는 2026-06-19 07:05 KST.
