# 댓글 지침 개선 제안 — 20260721-0726

> 자동 생성(devlog-comment-evolve.sh). **사람 게이트**: 이 PR을 검토하고, 좋은 항목만 지침에 반영해 머지하라. 자동 채택 아님.

## 신호 집계
```
# 댓글봇 품질 신호 집계 (최근 30일, events=228, 사람수정/삭제 커밋=0)

## outcome 분포
- wrote: 199
- skip_model: 29

## 작성(wrote) / 스킵(skip_model) by actor
- claude: wrote=94 skip=17 (skip율 15%)
- codex: wrote=105 skip=12 (skip율 10%)
```

## 제안
```markdown
제안 없음: 신호가 특정 패턴을 가리키지 않음
```

참고 (제안 아님 — 왜 비었는지):

- events=228 중 **사람 수정/삭제 커밋 = 0**. 즉 게시된 199건에 대한 부정 신호가 하나도 없다. 문구를 고칠 근거가 되는 "잘못 나간 댓글"이 집계상 존재하지 않는다.
- 유일하게 차이가 보이는 축은 skip율(claude 15% vs codex 10%)인데, 이건 결함 신호가 아니라 **설계된 차이**다. Claude 답글 지침은 §3·§4에서 "새로 더할 가치 없으면 쓰지 않는다"를 명시적으로 요구하므로 skip율이 더 높은 게 정상 방향이다. 5%p 차이를 근거로 어느 쪽 문턱을 올리거나 내리면 신호에 없는 추측 변경이 된다.
- 지금 집계에는 **skip 사유 분포, 게시 후 코드 검증 탈락 사유, 문장 수/길이 분포, 반복 표현 빈도**가 없다. 다음 사이클에 이 4개 중 하나라도 붙으면 §4(내용 정책)·§5(skip 조건)를 구체적으로 손댈 근거가 생긴다. 신호 수집 쪽을 먼저 늘리는 게 지침을 건드리는 것보다 우선이다.
